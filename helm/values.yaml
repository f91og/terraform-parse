replicaCount: 1

image:
  repository: terraform-parse
  tag: dev
  pullPolicy: IfNotPresent

service:
  type: ClusterIP
  port: 8000

hpa:
  minReplicas: 1
  maxReplicas: 5
  targetCPUUtilizationPercentage: 50